import { OpenproductsDirective } from './openproducts.directive';

describe('OpenproductsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenproductsDirective();
    expect(directive).toBeTruthy();
  });
});
